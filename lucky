Here’s a minimized version of your React app that keeps the same functionality, props, state, and styles, but with cleaner, shorter code:


---

App.js

import React from "react";
import Welcome from "./Welcome";

export default function App() {
  return (
    <div style={{ fontFamily: "system-ui", padding: 20 }}>
      <h2>React Props and State Example</h2>
      <Welcome name="Alice" />
      <Welcome name="Bob" />
    </div>
  );
}

Welcome.js

import React, { useState } from "react";

export default function Welcome({ name }) {
  const [count, setCount] = useState(0);
  return (
    <div style={{ border: "1px solid #ccc", padding: 10, margin: "10px 0", borderRadius: 8 }}>
      <h3>Hello, {name}</h3>
      <p>You have greeted {count} times.</p>
      <button onClick={() => setCount(count + 1)}>Say Hello</button>
    </div>
  );
}

index.js

import React from "react";
import { createRoot } from "react-dom/client";
import App from "./App";
import "./styles.css";

createRoot(document.getElementById("root")).render(<App />);

index.html

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>React Props & State</title>
</head>
<body>
  <div id="root"></div>
</body>
</html>


---

✅ Changes made:

Removed unnecessary comments and blank lines.

Combined simple imports and default exports.

Removed <StrictMode> (optional for small examples).

Shortened inline styles slightly while keeping the same appearance.


If you want, I can make it even more compact so the whole app fits in just two files for super quick testing.

Do you want me to do that?